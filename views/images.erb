<!DOCKTYPE html>
<html lang=ja>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <% @images_path.each do |images_path| %>
    <p><img src=<%= images_path %>></p>
    <% end %>
    <form method="post" action="/new">
      <input type="button" value="戻る" onClick="history.back()">
    </form>
  </body>
</html>